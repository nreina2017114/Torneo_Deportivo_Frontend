import { SearchforLPipe } from './searchfor-l.pipe';

describe('SearchforLPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchforLPipe();
    expect(pipe).toBeTruthy();
  });
});
